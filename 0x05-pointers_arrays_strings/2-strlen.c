#include "main.h"

/** 
* strlen  - finds the length of the word
*@s - the character passed into the function to check the length
*Return - length of the word
*
*/

int _strlen(char *s){
int length = 0;

while(*s++){
length++
}
return (length);
}
